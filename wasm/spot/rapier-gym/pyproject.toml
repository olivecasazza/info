[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "spot-rapier"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["gymnasium>=0.29", "numpy>=1.24"]

[tool.maturin]
python-source = "python"
features = ["pyo3/extension-module"]
