target/
Cargo.lock

# Python build / runtime artifacts (maturin develop output, pytest cache).
__pycache__/
*.so
*.pyd
.pytest_cache/

# Trained-policy normalization snapshots — emitted by training, not source.
../assets/obs_mean.npy
../assets/obs_std.npy
